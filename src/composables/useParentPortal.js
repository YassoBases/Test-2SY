/**
 * @deprecated Use useParentShell + page-specific composables instead.
 * Kept as alias so existing imports do not pull all parent APIs.
 */
export { useParentShell as useParentPortal } from './useParentShell.js'
